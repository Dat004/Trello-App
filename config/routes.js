import paths from "./paths";
import pages from "@/Pages";

const routes = [
  {
    id: 1,
    name: "home",
    path: paths.home,
    page: pages.Home,
  },
  {
    id: 1,
    name: "home",
    path: paths.board,
    page: pages.Board,
  },
];

export default routes;
